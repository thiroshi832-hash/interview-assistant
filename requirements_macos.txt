# ── UI ────────────────────────────────────────────────────────────────────
# PySide6 6.6+ only ships macOS 12+ wheels. 6.5.x is the last version with
# macOS 11 (Big Sur) universal2 wheels.
PySide6>=6.5,<6.6

# ── LLM providers (pick one in the app's API-key dialog; both bundled) ────
anthropic>=0.45
openai>=1.40

# ── Resume parsing ────────────────────────────────────────────────────────
pypdf>=4.0
python-docx>=1.1

# ── Audio capture (macOS: standard PyAudio + PortAudio via Homebrew) ──────
# Install PortAudio first: brew install portaudio
PyAudio>=0.2.14

# ── Network audio streaming (helper-network mode) ────────────────────────
websockets>=12.0

# ── DSP ───────────────────────────────────────────────────────────────────
# numpy 2.x links to macOS 14's Accelerate framework — crashes on macOS 11.
# numpy 1.26.x bundles OpenBLAS and has macOS 11 arm64 wheels.
numpy>=1.26,<2
scipy>=1.11,<1.14

# ── VAD ───────────────────────────────────────────────────────────────────
# silero-vad ONNX file is bundled in assets/. We hit it directly via onnxruntime.
# 1.20+ is built against the macOS 14 SDK and pulls in std::pmr symbols that
# Big Sur's libc++ doesn't export. 1.19.2 is the newest with real macOS 11
# binaries (LC_BUILD_VERSION minos=11.0). Verified by scripts/check_macos_min_version.py.
onnxruntime>=1.17,<1.20

# ── STT ───────────────────────────────────────────────────────────────────
pywhispercpp>=1.4               # whisper.cpp streaming engine (chunked partials)
deepgram-sdk>=3.7               # cloud streaming STT

# ── Speaker embeddings (helper-laptop mode only — single-mic diarization) ─
# Resemblyzer is NOT listed here. Use install_macos.sh which installs it
# with --no-deps. On macOS, webrtcvad can be built from source with Xcode
# Command Line Tools (xcode-select --install).
librosa>=0.10
scikit-learn>=1.1
